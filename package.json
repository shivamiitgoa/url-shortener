{
  "name": "url-shortener",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "test": "npm run --workspaces test",
    "lint": "npm run --workspaces lint",
    "typecheck": "npm run --workspaces typecheck",
    "dev:api": "npm --workspace @url-shortener/api-service run dev",
    "dev:redirect": "npm --workspace @url-shortener/redirect-service run dev",
    "dev:worker": "npm --workspace @url-shortener/events-worker run dev",
    "dev:web": "npm --workspace @url-shortener/web-dashboard run dev"
  },
  "devDependencies": {
    "@types/node": "^20.17.32",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=24"
  }
}
